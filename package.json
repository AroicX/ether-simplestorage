{
  "scripts": {
    "compile": "yarn solcjs --abi --bin --overwrite -o ./build contracts/*.sol"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0",
    "prettier": "^3.1.0",
    "prettier-plugin-solidity": "^1.2.0",
    "solc": "0.8.7-fixed"
  },
  "license": "MIT"
}
